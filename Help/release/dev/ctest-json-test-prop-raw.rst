ctest-json-test-prop-raw
------------------------

* The CTest option ``--show-only=[format]`` now has a ``json-v1-raw`` format.
  When this format is used, the ``value`` for each test property is the
  original unparsed string value specified in CMake.
